---
'@jstark/tplink-smarthome-api': patch
---

Bump `@commander-js/extra-typings` to 15.x to realign with the already-merged
`commander@15`, and adapt the dynamic `cli.ts` action handler to v15's stricter
typed callback signature.
