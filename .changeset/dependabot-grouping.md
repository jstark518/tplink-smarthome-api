---
'@jstark/tplink-smarthome-api': patch
---

Group peer-locked Dependabot updates (ESLint toolchain, commander + its typings)
so version-locked companions bump together across all update types. CI
configuration only — no changes to the published packages, so no release.
