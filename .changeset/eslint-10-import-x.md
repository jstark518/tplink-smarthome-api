---
'@jstark/tplink-smarthome-api': patch
---

Upgrade ESLint toolchain to v10 and replace `eslint-plugin-import` with the
maintained `eslint-plugin-import-x` fork (the former has no eslint@10 peer
support). Dev-tooling only — no changes to the published packages, so no release.
